#!/bin/sh

case "$1" in
	start)
		mount -t devpts none /dev/pts 2> /dev/null
		telnetd -l /bin/login
		;;
	stop)
		kill -15 `pidof telnetd`
		;;
	restart)
		$0 stop
		$0 start
		;;
	*)
		echo "Usage: $0 {start|stop|restart}"
		exit 1
		;;
esac

exit 0
