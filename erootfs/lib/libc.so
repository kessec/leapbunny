/* GNU ld script
 * Use the shared library, but some functions are only in
 * the static library, so try that secondarily. */
GROUP ( /opt/RidgeRun/arm-eabi-uclibc/lib/uclibc_nonshared.a /opt/RidgeRun/arm-eabi-uclibc/lib/libc.so.0  )
